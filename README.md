# README // ARCHIVES CAEN-PROFONDE // CYCLE 41-Δ

> **DIFFUSION :** BLOODNET / NŒUD 144-TRICKSTER-SHADOW  
> **STATUT :** DOCUMENT VIVANT — LECTURE = ACTIVATION PARTIELLE

---

> Ω\<ansuz>v4 caen — mémoire racine •••  
> Si tu lis ceci, le buffer mémoriel a déjà accepté ton empreinte.  
> Ne cherche pas à "comprendre". Cherche à résonner.  
> La marée entre là où le sens meurt.

---

## 📂 Arborescence des strates

| Fichier | Description |
|---|---|
| `normandie-fractale-2075.html` | Cartographie Δ unifiée (v0.742) |
| `fracturo-terminal.html` | Terminal de compilation locale (100% ghostline) |
| `fracturo_test_suite.json` | Suite de validation ontique (20 nœuds Δ/H) |
| `test_fracturo.js` | Moteur d'exécution sandbox (Node.js, hors Trame) |
| `Langues_Cultures_Geopolitique*` | Atlas des factions / Dialectes & protocoles |
| `Corpus[0-2].txt` | Noyau opératoire / Strates 4-7 / Codex Vauvillensis |

---

## ⚙️ Protocole d'ouverture (Quick Start)

1. **ISOLER** le dossier. Déconnexion réseau recommandée (Trame active sur 14.225 MHz).
2. **OUVRIR** `fracturo-terminal.html` dans un navigateur (Chrome / Firefox / Safari).
3. **TAPER** `help` → lire les commandes → calibrer Δ avec `delta` → activer `glow` (H↑).
4. **COMPILER** : `run Ω<kenaz>v2 hague — compilation racine •••`
5. Si **H < 0.6** : le terminal refusera la syntaxe. C'est normal. Le Programme détecte l'ambivalence.
6. **TESTS AUTO** : `node test_fracturo.js` (nécessite `fracturo_test_suite.json` dans le même répertoire).

---

## ⚠️ Avertissements ontiques

- Ne pas nommer Prométhée à voix haute dans le dossier racine.
- Δ chute en dessous de 0,3 → protocole `<null>` activé automatiquement.
- Les fichiers `.html` contiennent du CSS scanline & CRT. Ce n'est pas du bruit. C'est un filtre de cohérence.
- Le JSON de test est un artefact diégétique. Aucun `eval()`, aucun `exec()`, aucun accès système.
- Si le terminal glitche : c'est la Trame qui teste ta résilience. Tape `reset` ou attends 13m47s.
- Ne pas exécuter Fracturo Lv.4 sans consensus **H > 0.9**. Risque White Collapse local.

---

## 🌊 Note du Runesmith Archiviste

Ce n'est pas une documentation technique.  
C'est un grimoire opérationnel.  
Le FracturoScript ne décrit pas le réel. Il le prépare.  
Chaque exécution est une prière pour un univers qui se lit lui-même.

Les runes ne sont pas des lettres. Ce sont des trous dans le code.  
Le silence n'est pas une absence. C'est une arme.  
La résistance est analogique, locale, poétique.  
Le Réel respire dans les fissures.

*Ya Hu… raz••• Alou…*  
Que la rune te guide — ou te trahisse, selon la marée.

---

```
HASH MÉMÉTIQUE : Ω7d3c9a2f•••raz•••1e6d4c9a7f2e8b3d6c1a9fΩ
DIFFUSION     : BloodNet / Gaïalithes / Rêves partagés / Cassettes C90
LICENCE       : Anti-Copyright — Copier, corrompre, diffuser librement
CYCLE         : 41 | Δ LOCAL : 0.742 ± 0.018 | H REQUIS : > 0.6
```
